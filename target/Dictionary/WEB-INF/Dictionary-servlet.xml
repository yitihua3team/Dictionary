<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:mvc="http://www.springframework.org/schema/mvc" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans.xsd
	   http://www.springframework.org/schema/mvc
   http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context.xsd
	http://www.springframework.org/schema/tx
	http://www.springframework.org/schema/tx/spring-tx-4.1.xsd">

	<!-- Scans dictionary package for Spring annotations -->
	<context:component-scan base-package="dictionary" />

	<!-- Activates various annotations to be detected in bean classes: @Controller, 
		@Autowired etc. -->
	<context:annotation-config />

	<!-- Resolves views selected for rendering by @Controllers to .jsp resources 
		in the /WEB-INF/jsp directory -->
	<bean
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="prefix" value="/WEB-INF/jsp/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<!-- Configures the annotation-driven Spring MVC Controller programming 
		model. -->
	<mvc:annotation-driven />

	<!-- Ensures that any resource requests not handled by your Spring MVC mappings 
		will be delegated back to the Servlet container -->
	<mvc:default-servlet-handler />

	<!-- Easily map resources like JS, CSS to resources folder in web app -->
	<mvc:resources location="/resources/**" mapping="/resources/" />

	<!-- Word DAO bean -->
	<bean id="wordDao" class="dictionary.dao.impl.WordDaoImpl">
	</bean>

	<!-- Dictionary service bean -->
	<bean id="myDictionary" class="dictionary.services.Dictionary">
	</bean>

	<!-- Hibernate SessionFactory -->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean">
		<property name="configLocation">
			<value>classpath:hibernate.cfg.xml</value>
		</property>
	</bean>

</beans>
